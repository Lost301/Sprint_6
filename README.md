# Sprint_6

UI-тесты учебного сервиса «Яндекс.Самокат» на Selenium, pytest и Allure.

Установка: `pip install -r requirements.txt`

Запуск: `pytest --alluredir=allure_results`

Отчёт: `allure serve allure_results`
